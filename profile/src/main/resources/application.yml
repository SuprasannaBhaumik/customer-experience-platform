server:
  port: 8081

spring:
  application:
    name: profile
  datasource:
    url: jdbc:postgresql://localhost:5432/profile
    username: suprasannabhaumik
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
